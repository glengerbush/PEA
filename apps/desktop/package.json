{
	"name": "pea-desktop",
	"version": "1.2.1",
	"private": true,
	"scripts": {
		"dev": "tauri dev",
		"build": "echo 'desktop shell is built via tauri, not the workspace build'",
		"tauri": "tauri"
	},
	"devDependencies": {
		"@tauri-apps/cli": "^2.11.4"
	}
}
