# Defines the pnpm workspace for the monorepo
packages:
    - 'packages/*'
    - 'apps/*'

# Dependencies allowed to run their build/postinstall scripts (pnpm >=10 blocks
# all by default). esbuild (via vite) downloads its platform binary.
onlyBuiltDependencies:
    - esbuild
